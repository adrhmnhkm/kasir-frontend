{
  "version": 2,
  "builds": [
    {
      "src": "frontend-only/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "frontend-only/public"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/frontend-only/$1"
    }
  ]
}